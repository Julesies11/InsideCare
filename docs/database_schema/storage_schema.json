[
  {
    "bucket_name": "branch-documents",
    "policyname": "RBAC branch_documents SELECT",
    "operation": "SELECT",
    "roles": "{authenticated}",
    "using_expression": "((bucket_id = 'branch-documents'::text) AND (jwt_is_admin() OR (EXISTS ( SELECT 1\n   FROM houses h\n  WHERE (((h.branch_id)::text = split_part(objects.name, '/'::text, 1)) AND jwt_has_house(h.id))))))",
    "with_check_expression": null
  },
  {
    "bucket_name": "checklist-attachments",
    "policyname": "RBAC checklist_attachments INSERT",
    "operation": "INSERT",
    "roles": "{authenticated}",
    "using_expression": null,
    "with_check_expression": "((bucket_id = 'checklist-attachments'::text) AND (jwt_is_admin() OR (EXISTS ( SELECT 1\n   FROM house_checklist_submissions hcs\n  WHERE (((hcs.id)::text = split_part(objects.name, '/'::text, 1)) AND jwt_has_house(hcs.house_id) AND (jwt_get_perm('house_checklists'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text])))))))"
  },
  {
    "bucket_name": "checklist-attachments",
    "policyname": "RBAC checklist_attachments SELECT",
    "operation": "SELECT",
    "roles": "{authenticated}",
    "using_expression": "((bucket_id = 'checklist-attachments'::text) AND (jwt_is_admin() OR (EXISTS ( SELECT 1\n   FROM house_checklist_submissions hcs\n  WHERE (((hcs.id)::text = split_part(objects.name, '/'::text, 1)) AND jwt_has_house(hcs.house_id))))))",
    "with_check_expression": null
  },
  {
    "bucket_name": "checklist-attachments",
    "policyname": "RBAC storage_objects INSERT",
    "operation": "INSERT",
    "roles": "{authenticated}",
    "using_expression": null,
    "with_check_expression": "(jwt_is_admin() OR ((bucket_id = 'checklist-attachments'::text) AND (jwt_get_perm('house_checklists'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'participant-photos'::text) AND (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'participant-documents'::text) AND (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'staff-photos'::text) AND (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text)) OR ((bucket_id = 'staff-documents'::text) AND (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text)))"
  },
  {
    "bucket_name": "house-documents",
    "policyname": "RBAC house_documents INSERT",
    "operation": "INSERT",
    "roles": "{authenticated}",
    "using_expression": null,
    "with_check_expression": "((bucket_id = 'house-documents'::text) AND (jwt_is_admin() OR (jwt_has_house((split_part(name, '/'::text, 1))::uuid) AND (jwt_get_perm('houses'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text])))))"
  },
  {
    "bucket_name": "house-documents",
    "policyname": "RBAC house_documents SELECT",
    "operation": "SELECT",
    "roles": "{authenticated}",
    "using_expression": "((bucket_id = 'house-documents'::text) AND (jwt_is_admin() OR jwt_has_house((split_part(name, '/'::text, 1))::uuid) OR (jwt_get_perm('houses'::text) = ANY (ARRAY['full'::text, 'read_only'::text]))))",
    "with_check_expression": null
  },
  {
    "bucket_name": "participant-documents",
    "policyname": "RBAC participant_documents INSERT",
    "operation": "INSERT",
    "roles": "{authenticated}",
    "using_expression": null,
    "with_check_expression": "((bucket_id = 'participant-documents'::text) AND (jwt_is_admin() OR (EXISTS ( SELECT 1\n   FROM participants p\n  WHERE (((p.id)::text = split_part(objects.name, '/'::text, 1)) AND jwt_has_house(p.house_id) AND (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text])))))))"
  },
  {
    "bucket_name": "participant-documents",
    "policyname": "RBAC participant_documents SELECT",
    "operation": "SELECT",
    "roles": "{authenticated}",
    "using_expression": "((bucket_id = 'participant-documents'::text) AND (jwt_is_admin() OR (EXISTS ( SELECT 1\n   FROM participants p\n  WHERE (((p.id)::text = split_part(objects.name, '/'::text, 1)) AND (jwt_has_house(p.house_id) OR (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'read_only'::text]))))))))",
    "with_check_expression": null
  },
  {
    "bucket_name": "participant-documents",
    "policyname": "RBAC storage_objects INSERT",
    "operation": "INSERT",
    "roles": "{authenticated}",
    "using_expression": null,
    "with_check_expression": "(jwt_is_admin() OR ((bucket_id = 'checklist-attachments'::text) AND (jwt_get_perm('house_checklists'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'participant-photos'::text) AND (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'participant-documents'::text) AND (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'staff-photos'::text) AND (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text)) OR ((bucket_id = 'staff-documents'::text) AND (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text)))"
  },
  {
    "bucket_name": "participants",
    "policyname": "RBAC participant_documents INSERT",
    "operation": "INSERT",
    "roles": "{authenticated}",
    "using_expression": null,
    "with_check_expression": "((bucket_id = 'participant-documents'::text) AND (jwt_is_admin() OR (EXISTS ( SELECT 1\n   FROM participants p\n  WHERE (((p.id)::text = split_part(objects.name, '/'::text, 1)) AND jwt_has_house(p.house_id) AND (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text])))))))"
  },
  {
    "bucket_name": "participants",
    "policyname": "RBAC participant_documents SELECT",
    "operation": "SELECT",
    "roles": "{authenticated}",
    "using_expression": "((bucket_id = 'participant-documents'::text) AND (jwt_is_admin() OR (EXISTS ( SELECT 1\n   FROM participants p\n  WHERE (((p.id)::text = split_part(objects.name, '/'::text, 1)) AND (jwt_has_house(p.house_id) OR (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'read_only'::text]))))))))",
    "with_check_expression": null
  },
  {
    "bucket_name": "participants",
    "policyname": "RBAC participant_photos INSERT",
    "operation": "INSERT",
    "roles": "{authenticated}",
    "using_expression": null,
    "with_check_expression": "((bucket_id = 'participant-photos'::text) AND (jwt_is_admin() OR (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))))"
  },
  {
    "bucket_name": "participants",
    "policyname": "RBAC participant_photos SELECT",
    "operation": "SELECT",
    "roles": "{authenticated}",
    "using_expression": "((bucket_id = 'participant-photos'::text) AND (jwt_is_admin() OR (EXISTS ( SELECT 1\n   FROM participants p\n  WHERE (((p.id)::text = split_part(objects.name, '/'::text, 1)) AND (jwt_has_house(p.house_id) OR (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'read_only'::text, 'context_read_only'::text]))))))))",
    "with_check_expression": null
  },
  {
    "bucket_name": "participants",
    "policyname": "RBAC storage_objects INSERT",
    "operation": "INSERT",
    "roles": "{authenticated}",
    "using_expression": null,
    "with_check_expression": "(jwt_is_admin() OR ((bucket_id = 'checklist-attachments'::text) AND (jwt_get_perm('house_checklists'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'participant-photos'::text) AND (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'participant-documents'::text) AND (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'staff-photos'::text) AND (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text)) OR ((bucket_id = 'staff-documents'::text) AND (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text)))"
  },
  {
    "bucket_name": "staff-documents",
    "policyname": "RBAC staff_documents INSERT",
    "operation": "INSERT",
    "roles": "{authenticated}",
    "using_expression": null,
    "with_check_expression": "((bucket_id = 'staff-documents'::text) AND (jwt_is_admin() OR (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text) OR (jwt_get_perm('employees'::text) = 'full'::text)))"
  },
  {
    "bucket_name": "staff-documents",
    "policyname": "RBAC staff_documents SELECT",
    "operation": "SELECT",
    "roles": "{authenticated}",
    "using_expression": "((bucket_id = 'staff-documents'::text) AND (jwt_is_admin() OR (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text) OR (jwt_get_perm('employees'::text) = ANY (ARRAY['full'::text, 'read_only'::text])) OR jwt_manages_staff((split_part(name, '/'::text, 1))::uuid)))",
    "with_check_expression": null
  },
  {
    "bucket_name": "staff-documents",
    "policyname": "RBAC storage_objects INSERT",
    "operation": "INSERT",
    "roles": "{authenticated}",
    "using_expression": null,
    "with_check_expression": "(jwt_is_admin() OR ((bucket_id = 'checklist-attachments'::text) AND (jwt_get_perm('house_checklists'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'participant-photos'::text) AND (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'participant-documents'::text) AND (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'staff-photos'::text) AND (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text)) OR ((bucket_id = 'staff-documents'::text) AND (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text)))"
  }
]