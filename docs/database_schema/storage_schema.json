[
  {
    "bucket_context": "branch-documents",
    "policyname": "RBAC branch_documents SELECT",
    "operation": "SELECT",
    "roles": "{authenticated}",
    "using_expression": "((bucket_id = 'branch-documents'::text) AND (jwt_is_admin() OR (EXISTS ( SELECT 1\n   FROM houses h\n  WHERE (((h.branch_id)::text = split_part(objects.name, '/'::text, 1)) AND jwt_has_house(h.id))))))",
    "with_check_expression": null
  },
  {
    "bucket_context": "checklist-attachments",
    "policyname": "RBAC checklist_attachments INSERT",
    "operation": "INSERT",
    "roles": "{authenticated}",
    "using_expression": null,
    "with_check_expression": "((bucket_id = 'checklist-attachments'::text) AND (jwt_is_admin() OR (EXISTS ( SELECT 1\n   FROM house_checklist_submissions hcs\n  WHERE (((hcs.id)::text = split_part(objects.name, '/'::text, 1)) AND jwt_has_house(hcs.house_id) AND (jwt_get_perm('house_checklists'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text])))))))"
  },
  {
    "bucket_context": "checklist-attachments",
    "policyname": "RBAC checklist_attachments SELECT",
    "operation": "SELECT",
    "roles": "{authenticated}",
    "using_expression": "((bucket_id = 'checklist-attachments'::text) AND (jwt_is_admin() OR (EXISTS ( SELECT 1\n   FROM house_checklist_submissions hcs\n  WHERE (((hcs.id)::text = split_part(objects.name, '/'::text, 1)) AND jwt_has_house(hcs.house_id))))))",
    "with_check_expression": null
  },
  {
    "bucket_context": "checklist-attachments",
    "policyname": "RBAC storage_objects INSERT",
    "operation": "INSERT",
    "roles": "{authenticated}",
    "using_expression": null,
    "with_check_expression": "(jwt_is_admin() OR ((bucket_id = 'checklist-attachments'::text) AND (jwt_get_perm('house_checklists'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'participant-photos'::text) AND (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'participant-documents'::text) AND (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'staff-photos'::text) AND (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text)) OR ((bucket_id = 'staff-documents'::text) AND (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text)))"
  },
  {
    "bucket_context": "GLOBAL / ALL BUCKETS",
    "policyname": "RBAC storage_objects ALL (Admin)",
    "operation": "ALL",
    "roles": "{authenticated}",
    "using_expression": "jwt_is_admin()",
    "with_check_expression": null
  },
  {
    "bucket_context": "house-documents",
    "policyname": "RBAC house_documents INSERT",
    "operation": "INSERT",
    "roles": "{authenticated}",
    "using_expression": null,
    "with_check_expression": "((bucket_id = 'house-documents'::text) AND (jwt_is_admin() OR (jwt_has_house((split_part(name, '/'::text, 1))::uuid) AND (jwt_get_perm('houses'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text])))))"
  },
  {
    "bucket_context": "house-documents",
    "policyname": "RBAC house_documents SELECT",
    "operation": "SELECT",
    "roles": "{authenticated}",
    "using_expression": "((bucket_id = 'house-documents'::text) AND (jwt_is_admin() OR jwt_has_house((split_part(name, '/'::text, 1))::uuid) OR (jwt_get_perm('houses'::text) = ANY (ARRAY['full'::text, 'read_only'::text]))))",
    "with_check_expression": null
  },
  {
    "bucket_context": "participant-documents",
    "policyname": "RBAC participant_documents INSERT",
    "operation": "INSERT",
    "roles": "{authenticated}",
    "using_expression": null,
    "with_check_expression": "((bucket_id = 'participant-documents'::text) AND (jwt_is_admin() OR (EXISTS ( SELECT 1\n   FROM participants p\n  WHERE (((p.id)::text = split_part(objects.name, '/'::text, 1)) AND jwt_has_house(p.house_id) AND (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text])))))))"
  },
  {
    "bucket_context": "participant-documents",
    "policyname": "RBAC participant_documents SELECT",
    "operation": "SELECT",
    "roles": "{authenticated}",
    "using_expression": "((bucket_id = 'participant-documents'::text) AND (jwt_is_admin() OR (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'read_only'::text])) OR ((jwt_get_perm('participants'::text) = ANY (ARRAY['context_read_write'::text, 'context_read_only'::text])) AND (EXISTS ( SELECT 1\n   FROM participants p\n  WHERE (((p.id)::text = split_part(objects.name, '/'::text, 1)) AND jwt_has_house(p.house_id)))))))",
    "with_check_expression": null
  },
  {
    "bucket_context": "participant-documents",
    "policyname": "RBAC storage_objects INSERT",
    "operation": "INSERT",
    "roles": "{authenticated}",
    "using_expression": null,
    "with_check_expression": "(jwt_is_admin() OR ((bucket_id = 'checklist-attachments'::text) AND (jwt_get_perm('house_checklists'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'participant-photos'::text) AND (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'participant-documents'::text) AND (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'staff-photos'::text) AND (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text)) OR ((bucket_id = 'staff-documents'::text) AND (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text)))"
  },
  {
    "bucket_context": "participant-photos",
    "policyname": "RBAC participant_photos INSERT",
    "operation": "INSERT",
    "roles": "{authenticated}",
    "using_expression": null,
    "with_check_expression": "((bucket_id = 'participant-photos'::text) AND (jwt_is_admin() OR (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))))"
  },
  {
    "bucket_context": "participant-photos",
    "policyname": "RBAC participant_photos SELECT",
    "operation": "SELECT",
    "roles": "{authenticated}",
    "using_expression": "((bucket_id = 'participant-photos'::text) AND (jwt_is_admin() OR (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'read_only'::text])) OR ((jwt_get_perm('participants'::text) = ANY (ARRAY['context_read_write'::text, 'context_read_only'::text])) AND (EXISTS ( SELECT 1\n   FROM participants p\n  WHERE ((((p.id)::text = split_part(objects.name, '/'::text, 1)) OR (p.photo_url ~~* ('%'::text || objects.name))) AND jwt_has_house(p.house_id)))))))",
    "with_check_expression": null
  },
  {
    "bucket_context": "participant-photos",
    "policyname": "RBAC storage_objects INSERT",
    "operation": "INSERT",
    "roles": "{authenticated}",
    "using_expression": null,
    "with_check_expression": "(jwt_is_admin() OR ((bucket_id = 'checklist-attachments'::text) AND (jwt_get_perm('house_checklists'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'participant-photos'::text) AND (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'participant-documents'::text) AND (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'staff-photos'::text) AND (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text)) OR ((bucket_id = 'staff-documents'::text) AND (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text)))"
  },
  {
    "bucket_context": "staff-documents",
    "policyname": "RBAC staff_documents INSERT",
    "operation": "INSERT",
    "roles": "{authenticated}",
    "using_expression": null,
    "with_check_expression": "((bucket_id = 'staff-documents'::text) AND (jwt_is_admin() OR (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text) OR (jwt_get_perm('employees'::text) = 'full'::text)))"
  },
  {
    "bucket_context": "staff-documents",
    "policyname": "RBAC staff_documents SELECT",
    "operation": "SELECT",
    "roles": "{authenticated}",
    "using_expression": "((bucket_id = 'staff-documents'::text) AND (jwt_is_admin() OR (jwt_get_perm('employees'::text) = ANY (ARRAY['full'::text, 'read_only'::text])) OR (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text) OR ((jwt_get_perm('employees'::text) = ANY (ARRAY['context_read_write'::text, 'context_read_only'::text])) AND (EXISTS ( SELECT 1\n   FROM staff s\n  WHERE (((s.id)::text = split_part(objects.name, '/'::text, 1)) AND jwt_manages_staff(s.id)))))))",
    "with_check_expression": null
  },
  {
    "bucket_context": "staff-documents",
    "policyname": "RBAC storage_objects INSERT",
    "operation": "INSERT",
    "roles": "{authenticated}",
    "using_expression": null,
    "with_check_expression": "(jwt_is_admin() OR ((bucket_id = 'checklist-attachments'::text) AND (jwt_get_perm('house_checklists'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'participant-photos'::text) AND (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'participant-documents'::text) AND (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'staff-photos'::text) AND (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text)) OR ((bucket_id = 'staff-documents'::text) AND (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text)))"
  },
  {
    "bucket_context": "staff-photos",
    "policyname": "RBAC staff_photos INSERT",
    "operation": "INSERT",
    "roles": "{authenticated}",
    "using_expression": null,
    "with_check_expression": "((bucket_id = 'staff-photos'::text) AND (jwt_is_admin() OR (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text) OR (jwt_get_perm('employees'::text) = 'full'::text)))"
  },
  {
    "bucket_context": "staff-photos",
    "policyname": "RBAC staff_photos SELECT",
    "operation": "SELECT",
    "roles": "{authenticated}",
    "using_expression": "((bucket_id = 'staff-photos'::text) AND (jwt_is_admin() OR (jwt_get_perm('employees'::text) = ANY (ARRAY['full'::text, 'read_only'::text])) OR (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text) OR ((jwt_get_perm('employees'::text) = ANY (ARRAY['context_read_write'::text, 'context_read_only'::text])) AND (EXISTS ( SELECT 1\n   FROM (house_staff_assignments hsa\n     JOIN staff s ON ((s.id = hsa.staff_id)))\n  WHERE ((((hsa.staff_id)::text = split_part(objects.name, '/'::text, 1)) OR (s.photo_url ~~* ('%'::text || objects.name))) AND jwt_has_house(hsa.house_id)))))))",
    "with_check_expression": null
  },
  {
    "bucket_context": "staff-photos",
    "policyname": "RBAC storage_objects INSERT",
    "operation": "INSERT",
    "roles": "{authenticated}",
    "using_expression": null,
    "with_check_expression": "(jwt_is_admin() OR ((bucket_id = 'checklist-attachments'::text) AND (jwt_get_perm('house_checklists'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'participant-photos'::text) AND (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'participant-documents'::text) AND (jwt_get_perm('participants'::text) = ANY (ARRAY['full'::text, 'context_read_write'::text]))) OR ((bucket_id = 'staff-photos'::text) AND (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text)) OR ((bucket_id = 'staff-documents'::text) AND (split_part(name, '/'::text, 1) = (jwt_get_staff_id())::text)))"
  }
]